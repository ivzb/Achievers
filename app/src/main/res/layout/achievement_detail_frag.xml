<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:adapter="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="android.view.View" />
        <import type="com.achievers.util.DateUtils" />

        <variable
            name="actionHandler"
            type="com.achievers.AchievementDetail.AchievementDetailContract.Presenter" />

        <variable
            name="viewModel"
            type="com.achievers.AchievementDetail.AchievementDetailViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:paddingBottom="@dimen/activity_vertical_margin"
            android:paddingLeft="@dimen/activity_horizontal_margin"
            android:paddingRight="@dimen/activity_horizontal_margin"
            android:paddingTop="@dimen/activity_vertical_margin"
            android:visibility="@{ viewModel.achievement == null ? View.VISIBLE : View.GONE}">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/no_data"
                android:textAppearance="?android:attr/textAppearanceLarge" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:visibility="@{ viewModel.achievement == null ? View.GONE : View.VISIBLE}">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{ viewModel.achievement.title }" />

            <com.achievers.util.ScrollChildSwipeRefreshLayout
                android:id="@+id/refresh_layout"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                adapter:onRefresh="@{ actionHandler }"
                adapter:achievement="@{ viewModel.achievement }">

                <RelativeLayout
                    android:id="@+id/evidenceContainer"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clickable="true"
                    android:orientation="vertical">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:orientation="vertical">
                        <!--android:visibility="@{ operatorCard.operationMeasurements.size() > 0 ? View.VISIBLE : View.GONE}"-->

                        <android.support.v7.widget.RecyclerView
                            android:id="@+id/rvEvidence"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            app:rvEvidenceAdapter="@{ viewModel.evidenceAdapter }" />
                    </LinearLayout>

                    <LinearLayout
                        android:id="@+id/noEvidence"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_centerInParent="true"
                        android:orientation="vertical">
                        <!--android:visibility="@{categories.notEmpty ? View.GONE : View.VISIBLE}"-->

                        <!--<ImageView-->
                        <!--android:id="@+id/noCategoriesIcon"-->
                        <!--android:layout_width="48dp"-->
                        <!--android:layout_height="48dp"-->
                        <!--android:layout_gravity="center"-->
                        <!--android:src="@{categories.noCategoriesIconRes}" />-->

                        <!--<TextView-->
                        <!--android:id="@+id/noCategoriesMain"-->
                        <!--android:layout_width="wrap_content"-->
                        <!--android:layout_height="wrap_content"-->
                        <!--android:layout_gravity="center"-->
                        <!--android:text="@{categories.noCategoriesLabel}"-->
                        <!--android:layout_marginBottom="@dimen/list_item_padding"/>-->

                    </LinearLayout>
                </RelativeLayout>
            </com.achievers.util.ScrollChildSwipeRefreshLayout>
        </LinearLayout>
    </LinearLayout>
</layout>
