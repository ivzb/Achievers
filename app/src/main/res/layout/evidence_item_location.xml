<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="com.achievers.util.DateUtils" />

        <variable
            name="evidence"
            type="com.achievers.data.models.Evidence" />

        <variable
            name="actionHandler"
            type="com.achievers.Evidence.EvidenceItemActionHandler" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#ccc"
        android:padding="5dp"
        android:layout_margin="5dp"
        android:onClick="@{ () -> actionHandler.evidenceClicked(evidence) }">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{ evidence.comment }"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="40sp"
                android:text="@{ `Location: ` + evidence.url }"/>

        </LinearLayout>
    </LinearLayout>
</layout>